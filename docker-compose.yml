services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      # SQLite-база живёт на хосте и переживает пересборку образа
      - ./instance:/app/instance
    restart: unless-stopped
